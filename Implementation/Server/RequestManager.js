
// Not finished, very early stages 
// 3 parameters: Graph (ex. 3 points), boolean (isScheduled), time interval
/*
function requestTimeToRun () {

    // Checks if the device is already scheduled
    if (isScheduled === true) {
        removeDemand();
    }
    else if (isScheduled === false) {
        gatherSurplusGraph();
    }

}
*/